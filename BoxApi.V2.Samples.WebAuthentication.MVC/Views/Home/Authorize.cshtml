@model BoxApi.V2.Authentication.OAuth2.OAuthToken

@{
    ViewBag.Title = "Authorization Complete";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Authorization Complete</h2>

<table>
    <tr>
        <td><strong>Access Token:</strong></td>
        <td style="padding-left:10px">@Model.AccessToken</td>
    </tr>
    <tr>
        <td><strong>Refresh Token:</strong></td>
        <td style="padding-left:10px">@Model.RefreshToken</td>
    </tr>

</table>

<h3>Want to run the unit tets?</h3>

<p>If you have cloned the <a href="https://github.com/jhoerr/box-csharp-sdk-v2">Box v2 API repo</a>, you can use this token for unit testing.  @Html.ActionLink("Save the token", "Save") and copy the resulting <strong>test_info.json</strong> file into the root of the BoxApi.V2.Tests folder, alongside the <em>.csproj</em> file.  (The file is already .gitignored so it's won't be checked in.)</p>